package huynguyen.com.MXHApp.Fragments;

import androidx.fragment.app.Fragment;

/**
 * A placeholder fragment that is used in the navigation graph for the 'Post' menu item.
 * It does not display any UI. Its sole purpose is to intercept the click event in HomeActivity
 * so we can launch the PostActivity.
 */
public class PostPlaceholderFragment extends Fragment {
}
